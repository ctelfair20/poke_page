import * as React from 'react';

interface Props {
}

const Actions = (props: Props) => {
  return (
    <div className="pokemon-actions">The pokemon ACTIONS go there!!</div>
  );
}

export default Actions;